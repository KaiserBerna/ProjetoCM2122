<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ipca.projetocm.mygarden">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyGarden">
        <activity android:name=".MainApp.MainMenuActivity"></activity>
        <activity android:name=".LoginSignup.SignUpActivity"></activity>
        <activity android:name=".MainApp.ManageGardenActivity"></activity>
        <activity android:name=".MainApp.EditSlotActivity"></activity>
        <activity android:name=".AccountSettingsAndInfo.AccountSettingsActivity"></activity>
        <activity android:name=".MainApp.AppInfoActivity"></activity>





        <activity
            android:name=".LoginSignup.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>